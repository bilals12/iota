apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: iota-state
  namespace: security
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  # Use a StorageClass that exists on the cluster (many EKS setups only ship gp2 by default).
  storageClassName: gp2
